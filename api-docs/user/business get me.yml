info:
  name: business get me
  type: http
  seq: 2

http:
  method: GET
  url: "{{base-api-url}}/users/me"
  headers:
    - name: accept
      value: application/json
  auth:
    type: bearer
    token: "{{access-token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
