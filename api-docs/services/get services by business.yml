info:
  name: get services by business
  type: http
  seq: 4

http:
  method: GET
  url: "{{base-api-url}}/services/by-business?page=1&perPage=15"
  params:
    - name: page
      value: "1"
      type: query
    - name: perPage
      value: "15"
      type: query
  auth:
    type: bearer
    token: "{{business-access-token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
