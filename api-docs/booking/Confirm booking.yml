info:
  name: Confirm booking
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{base-api-url}}/bookings/business/230fe521-bba0-417d-93a4-c122ac513005/status"
  body:
    type: json
    data: |-
      {
        "status": "confirmed"
      }
  auth:
    type: bearer
    token: "{{business-access-token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
