info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{base-api-url}}/auth/login"
  headers:
    - name: accept
      value: "*/*"
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "neuporng123@gmail.com",
        "password": "P@$$w0rd120320"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const response = res.getBody();

        bru.setEnvVar("access-token", response.accessToken)
        bru.setEnvVar("refresh-token", response.refreshToken)

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
